import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.nakay.shootinggame2',
  appName: 'anpanman-shooting',
  webDir: 'dist'
};

export default config;
